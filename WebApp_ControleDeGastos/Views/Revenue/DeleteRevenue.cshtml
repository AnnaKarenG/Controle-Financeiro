@model Revenue
@{
    ViewData["Title"] = "Exclusão de Receita";
}

<div class="text-center">
    <h3>Excluir a receita(@Model.RevenueId) </h3>
</div>


<div class="text-center" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Dejesa realmente excluir a receita?</h5>
            </div>
            <div>
               <br />
                <form asp-controller="Revenue" asp-action="DeleteRecordRevenue" method="post">
                    <input type="hidden" asp-for="@Model.RevenueId" name="id"/>

                    <button class="btn btn-danger"  type="submit">Excluir</button>
                    <a role="button" class="btn btn-secondary" asp-controller="Revenue" asp-action="Index">Voltar</a>

                </form>
                <br />
            </div>
        </div>
    </div>
</div>
